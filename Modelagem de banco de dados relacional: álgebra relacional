Para consolidar os conteúdos, abaixo temos uma série de demandas solicitadas que podem ser visualizadas através da álgebra relacional, o desafio é montar uma consulta utilizando seleção e projeção para obter informações específicas sobre os livros, vendedores, clientes e vendas.

Quais são os nomes dos livros que possuem preço maior que R$50?
Quais são os vendedores que possuem mais de 5 anos de experiência?
Quais são os códigos de livros recomendados pela revista ‘BestBooks Magazine’?
Quais são os livros com estoque menor que 20 unidades?
Quais são os códigos dos livros mais vendidos durante o ano de 2021?
Quais são os livros com preço entre R$30 e R$50?
Existe algum livros sem exemplar em estoque? Quais?
Quais são os livros que custam mais de R$ 50 e contém mais de 5 unidades no estoque?
Quais livros atendem os requisitos do amigo oculto do cliente André: ser um livro de George Orwell ou de qualquer outra autoria que seja mais caro que 30 reais?



------------------------------------------------------------------

Ex 1

π nome_livro (σ preco > 50 (Livros))

Ex 2

π nome (σ anos_exp > 5 (Vendedores))

Ex 3

π id_livro (LivrosRecomendados)

Ex 4

σ qtd_estoque < 20 (Livros)

Ex 5

π id_livro (σ ano = 2021 (LivroMaisVendidos))

Ex 6

π nome_livro (σ preco > 30 ∧ preco < 50 (Livros))

Ex 7

π nome_livro (σ qtd_estoque < 1 (Livros))

Ex 8

π nome_livro (σ preco > 50 ∧ qtd_estoque > 5 (Livros))

Ex 9

π nome_livro (σ preco > 30 ∨ autor = 'George Orwell' (Livros))

day 2

Monte consultas utilizando álgebra relacional que respondam às demandas abaixo:

Qual o nome e preço dos livros que custam até 19 reais?
Quais livros da “Colleen Houck” venderam mais de 3 exemplares em um único pedido?
Quais são os livros que tiveram vendas acima de 10 unidades em um único pedido?
Qual o nome dos livros mais recomendados com nota média superior a 4.3?


----------------------------------------------------------------

Ex 1 - π nome_livro, preco (σ preco <= 19 (Livros))

Ex 2 - π nome_livro (
    σ qtd_vendida > 3 (
        (σ autor = 'Colleen Houck' (Livros)) ⨝ Vendas
    )
)

ex 3 - π nome_livro (σ qtd_vendida > 3 ( Livros ⟕ Vendas ))

Ex 4 - π nome_livro (σ LivrosRecomendados.nota_media > 4.3 ( Livros ⨝ LivrosRecomendados))

.


day 3 

-- 1
π fname, lname (σ gender = 'F' (Actor));

-- 2
π name (σ year > 1999 (Movie));

-- 3
π Movie.name, Director.fname, Director.lname (
    Movie ⨝ Movie.id = Directs.mid
    Directs ⨝ Directs.did = Director.id
);

-- 4
π Movie.name, Actor.fname, Actor.lname, Acts.role (
    σ rank > 6 (Movie)
    ⨝ Movie.id = Acts.mid
    Acts ⨝ Acts.aid = Actor.id
);

-- 5
γ Director.id, Director.fname, Director.lname, COUNT(mid)→num_filmes (
    Director ⨝ Director.id = Directs.did Directs
);

-- 6
γ genre, COUNT(mid)→num_filmes (Genre);

-- 7
γ genre, AVG(rank)→media, MIN(rank)→min, MAX(rank)→max (
    Genre ⨝ Genre.mid = Movie.id Movie
);

-- 8
π fname, lname (Actor ⨝ Actor.id = Director.id Director);

-- 9
π fname, lname (Actor)
−
π fname, lname (Actor ⨝ Actor.id = Director.id Director);

-- 10
π fname, lname (Actor)
∪
π fname, lname (Director);

-- 11
π name (Movie)
−
π name (
    Movie ⨝ Movie.id = Directs.mid Directs
);

-- 12
π fname, lname (
    Actor ⨝ Actor.id = aid
    σ num_filmes < 2 (
        γ aid, COUNT(mid)→num_filmes (Acts)
    )
);

-- 13
γ genre, year, COUNT(mid)→media_filmes (
    (σ num_atores < 2 (
        γ mid, COUNT(aid)→num_atores (Acts)
    ))
    ⨝ mid = Movie.id Movie
    ⨝ mid = Genre.mid Genre
);
